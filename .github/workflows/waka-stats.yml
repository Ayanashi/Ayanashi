name: WakaTime Stats Update

on:
  schedule:
    - cron: '0 0 * * *'  # Esegue ogni giorno a mezzanotte UTC
  workflow_dispatch:       # Permette l'attivazione manuale

jobs:
  update-readme:
    name: Update Readme with WakaTime Metrics
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@8e5e7e5ab8b370d6c329ec480221332ada57f0ab  # v3.5.2
        
      - name: Update README with WakaTime Stats
        uses: athul/waka-readme@d3a214cce44dda21a17ce5d1f9735d6cf0f83a79  # Commit recente
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          SHOW_TITLE: "true"
          SHOW_TIME: "true"
          LANG_COUNT: "5"
          TIME_RANGE: "last_7_days"
          SHOW_MASKED_TIME: "false"
          
      - name: Commit and Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add README.md
          git commit -m "Update WakaTime stats" || echo "No changes to commit"
          git push
